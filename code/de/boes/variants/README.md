# Directory with source codes for different tested BOES variants
Paths in all present files assume that the file is located in `code/de/boes/`.

Move a variant file into the parent folder before running.

